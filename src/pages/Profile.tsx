import { LogOut, Moon, ShieldCheck } from 'lucide-react'
import { supabase } from '../lib/supabase'
import { useNavigate } from 'react-router-dom'
export function Profile() { const nav = useNavigate(); return <section className="max-w-2xl"><h1 className="text-2xl font-bold">Perfil e preferências</h1><div className="card mt-6 space-y-5 p-6"><div><h2 className="font-bold">Metas diárias</h2><p className="mt-1 text-sm text-slate-600">Ajuste calorias, macronutrientes, fibras e água conforme sua orientação.</p><button className="btn btn-soft mt-3">Editar metas</button></div><hr className="border-slate-100"/><div className="flex items-center justify-between"><div className="flex gap-3"><Moon className="text-slate-500"/><div><h2 className="font-semibold">Aparência</h2><p className="text-sm text-slate-500">Modo claro ou escuro</p></div></div><button className="btn btn-soft">Alternar</button></div><hr className="border-slate-100"/><div className="flex gap-3"><ShieldCheck className="text-brand"/><div><h2 className="font-semibold">Privacidade</h2><p className="text-sm text-slate-500">Seus dados são protegidos por políticas de acesso por usuário.</p></div></div><button onClick={async () => { await supabase?.auth.signOut(); nav('/entrar') }} className="btn btn-danger"><LogOut size={16}/> Sair da conta</button></div></section> }
